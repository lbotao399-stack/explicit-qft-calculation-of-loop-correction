# N=1 SYM (Euclidean) index

- `component_diagram_master_targets.md`
  - `N=1 SYM (Euclidean)` component 图页的统一 freeze、classical target 总表、decorated slot 规则、local selection rule，以及与 superspace track 的边界。
- `conventions_and_rules.md`
  - 当前 `N=1 SYM (Euclidean)` 计算实际调用的记号、field content、raw SUSY、free EOM、`Q_-`-BPS letters 和 Feynman rules。
- `current_divergence_working_convention.md`
  - 当前 `N=1 SYM (Euclidean)` 在换框架后的工作插入、current 投影、diagrammatics 保留项与 local extraction 约定。

当前两条工作轨：

- component current-diagram track
  - source of truth: `component_diagram_master_targets.md`
  - fixed convention: `Q_1\equiv Q_-`, `\kappa_A=i/2`, `\partial_\mu J_-^\mu` only as WT insertion
- superspace track
  - 只做 dictionary / anomaly multiplet bookkeeping
  - 不把 superspace anomaly equation 回写到 component divergence pages
